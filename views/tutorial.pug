extends layout.pug
block content
    div(class="container")
        h1 #{data.tutorial.subtitleOne}
        h3(class="center-align") #{data.tutorial.subtitleTwo}
        p #{data.tutorial.parOne} 
        p(class="center-align") 
            img(src="/images/tutorial/data_menu.png")
        p #{data.tutorial.parTwo}
        p(class="center-align")
            img(src="/images/tutorial/string_format.png")
        p #{data.tutorial.parThree}
        p #{data.tutorial.parFour}
        p #{data.tutorial.parFive}
        h3(class="center-align") #{data.tutorial.subtitleThree}
        p(class="center-align") 
            img(src="/images/tutorial/chart_menu.png")
        p #{data.tutorial.parSix}
        div(class="adhor")
            <!-- Yandex.RTB R-A-337737-2 -->
            div(id="yandex_rtb_R-A-337737-2")
            
        h3(class="center-align") #{data.tutorial.subtitleFour}
        p #{data.tutorial.parSeven}
        p(class="center-align")
            img(src="/images/tutorial/trading_menu.png")
        p #{data.tutorial.parEight}
        p #{data.tutorial.parNine}
        p #{data.tutorial.parTen}
          p(class="center-align")
            img(src="/images/tutorial/terminal_view.png")
        p #{data.tutorial.parEleven}
        script.
            (function(w, d, n, s, t) {
                w[n] = w[n] || [];
                w[n].push(function() {
                    Ya.Context.AdvManager.render({
                        blockId: "R-A-337737-2",
                        renderTo: "yandex_rtb_R-A-337737-2",
                        async: true
                    });
                });
                t = d.getElementsByTagName("script")[0];
                s = d.createElement("script");
                s.type = "text/javascript";
                s.src = "//an.yandex.ru/system/context.js";
                s.async = true;
                t.parentNode.insertBefore(s, t);
            })(this, this.document, "yandexContextAsyncCallbacks");